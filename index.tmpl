<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<link href="/favicon.ico" rel="shortcut icon" type="image/x-icon" />
<title>Alexey Michurin | http://michurin.github.io/</title>
<style>
body {
  font-family: Frutiger, "Frutiger Linotype", Univers, Calibri, "Gill Sans", "Gill Sans MT", "Myriad Pro", Myriad, "DejaVu Sans Condensed", "Liberation Sans", "Nimbus Sans L", Tahoma, Geneva, "Helvetica Neue", Helvetica, Arial, sans-serif;
  background: #eee;
  text-shadow: 0 1px 0 #fff;
  color: #000;
  padding: 0;
  margin: 3em;
}
h1, h2, h3 {
  color: #222;
}
a {
  text-decoration: none;
  color: #007;
}
a:hover {
  text-decoration: underline;
}
.zero {
  color: #777
}
</style>
</head>
<body>
  <h1>Alexey Michurin</h1>
  <h2>Projects</h2>
  <table>
  {%- for section in data.sections %}
    <tr><td colspan="3">
      <h3>{{ section.title }}</h3>
      <p>{{ section.desc }}</p>
    </td></tr>
    {%- for repo in section.repos %}
    <tr valign="top">
      <td align="right"{% if git[repo].stars == 0 %} class="zero"{% endif %} title="stars">{{ git[repo].stars }}</td>
      <td align="right"{% if git[repo].forks == 0 %} class="zero"{% endif %} title="forks">{{ git[repo].forks }}</td>
      <td><a target="_blank" href="https://github.com/michurin/{{ repo }}">{{ repo }}</a> [{{ git[repo].lang }}
      {%- if repo in cov %}, <a target="_blank" href="https://app.codecov.io/gh/michurin/{{ repo }}">{{ cov[repo].coverage }}%</a>
      {%- endif -%}
      {%- if repo in data.gh_page %}, <a target="_blank" href="https://michurin.github.io/{{ repo }}/" title="online">page</a>{% endif %}
      {%- if repo in data.webstore %}, <a target="_blank" href="{{ data.webstore[repo] }}">webstore</a>{% endif -%}
      {%- if repo in data.googlecode %}, <a target="_blank" href="{{ data.googlecode[repo] }}" title="archive">googlecode</a>{% endif -%}
      ]: {{ git[repo].desc }}</td>
    </tr>
    {%- endfor %}
  {%- endfor %}
  </table>
</body>
</html>
